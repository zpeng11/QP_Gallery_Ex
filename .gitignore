# QP_Gallery_Ex
# Keep as source-of-truth: Makefile, scripts/, patches/, docs/, README.md, toolchain.lock, original/*.url

# -----------------------------------------------------------------------------
# Long-lived generated artifacts (reproducible; do not commit)
# -----------------------------------------------------------------------------
# Local pinned toolchain payloads, wrappers, caches, and extracted binaries
.tooling/

# Rebuilt APK outputs and signing artifacts
build/

# Decompiled workspace generated by apktool
decoded/

# JADX generated outputs and indexes (analysis docs are kept)
analysis/jadx/

# Downloaded upstream app binaries (URL files are tracked instead)
original/*.apk
original/*.apks
original/*.xapk
original/*.zip

# -----------------------------------------------------------------------------
# Short-lived temporary/local state (do not commit)
# -----------------------------------------------------------------------------
# Logs and temporary files
*.log
*.tmp
*.temp
*.bak
*.orig

# Local editor/OS files
.DS_Store
Thumbs.db
*~
*.swp
*.swo

# IDE/workspace local settings
.idea/
.vscode/

# Local environment files
.env
.env.*
!.env.example

# Local build caches
.cache/

# Python/Node misc caches (safe no-op if unused)
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
